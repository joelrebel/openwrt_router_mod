openwrt mods, to handle powercuts, turn on, off PC.
The router ends up as a gateway when theres a power cut, powers up(via gpio pins) the homeserver when the power gets back.
It acts as a HA gateway, using a bunch of bash scripts :>
